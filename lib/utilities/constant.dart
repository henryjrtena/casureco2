import 'package:flutter/material.dart';

// Constant Values

// Int and Double
const defaultZero = 0;
const defaultZeroDouble = 0.0;
const defaultOne = 1;
const defaultOneDouble = 0.0;

// String
const appName = 'CASURECO II';
const emptyString = '';

// Colors
const white = Colors.white;
const black = Colors.black54;

// -> Color Pallet
const yellow = Color.fromRGBO(244, 235, 26, 1);
const blue = Color.fromRGBO(42, 48, 129, 1);
const red = Colors.redAccent;

// Validations
const minChars = 4;